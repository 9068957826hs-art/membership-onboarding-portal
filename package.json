{
  "name": "membership-portal-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend",
    "build": "npm run install-all",
    "start": "node backend/server.js"
  }
}
